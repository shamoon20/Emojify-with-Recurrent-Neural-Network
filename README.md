# Emojify-with-Recurrent-Neural-Network
PyTorch implementation of an LSTM network for predicting emojis from text sentences. Compares one-hot encoding with GloVe word embeddings across 5 emotion categories (love, sports, happiness, sadness). Features sequence handling with padding/truncation, custom model architecture, and full training pipeline. Demonstrates NLP fundamentals with RNNs.
